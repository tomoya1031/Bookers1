<div class="edit">
	<h2>Editing Book</h2>
	<%= form_for(@book) do |f| %>
		<% if @book.errors.any? %>
	        <div class="error">
	            <h4 class="error-title"><%= @book.errors.count %>errors prohibited this book from being saved:</h5>
		            <ul class="error-box">
			            <% @book.errors.full_messages.each do |message| %>
			                <li><%= message %></li>
                        <% end %>
                </ul>
            </div>
        <% end %>
        <div class="box field">
        	<div>
		        <%= f.label :title, "Titele" %>
		    </div>
		    <div>
		        <%= f.text_field :title %>
		    </div>
		</div>
		<div class="box">
			<div>
		        <%= f.label :title, "Titele" %>
		    </div>
		    <div>
		        <%= f.text_area :body %>
		    </div>
		</div>
	    <%= f.submit 'Update Book' ,class:"submit"%>
	    <%= link_to "Show", book_path(@book) ,class:"action" %>
	    <p class="action"> | </p>
	    <a class="action" href="/books">Back</a>
<% end %>
</div>